const Sequelize = require('sequelize');
module.exports = (sequelize, DataTypes) => {
  return tamanhos.init(sequelize, DataTypes);
}

class tamanhos extends Sequelize.Model {
  static init(sequelize, DataTypes) {
  return super.init({
    idtamanho: {
      autoIncrement: true,
      type: DataTypes.INTEGER,
      allowNull: false,
      primaryKey: true
    },
    nome: {
      type: DataTypes.STRING(50),
      allowNull: false
    },
    idcategoria: {
      type: DataTypes.INTEGER,
      allowNull: true,
      references: {
        model: 'categorias',
        key: 'idcategoria'
      }
    }
  }, {
    sequelize,
    tableName: 'tamanhos',
    schema: 'public',
    timestamps: false,
    indexes: [
      {
        name: "tamanhos_pkey",
        unique: true,
        fields: [
          { name: "idtamanho" },
        ]
      },
    ]
  });
  }
}
